name: property_management_analytics
version: 1.0.0
config-version: 2
profile: property_management_analytics
model-paths: ["models"]
test-paths: ["tests"]
clean-targets: ["target", "dbt_packages"]

vars:
  as_of_date: "2026-06-30"

models:
  property_management_analytics:
    staging:
      +schema: staging
      +materialized: view
    base:
      +schema: base
      +materialized: view
    intermediate:
      +schema: conformed
      +materialized: table
      operations:
        +schema: operations
    marts:
      +schema: marts
      +materialized: table
      core:
        +schema: core
        +materialized: table
      financial:
        +schema: financial
      leasing:
        +schema: leasing
      operations:
        +schema: operations
